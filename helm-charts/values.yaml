# Default values for kibble.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ghcr.io/taonic/kibble
  tag: latest
  imagePullPolicy: Always

prom_endpoint: https://a2dd6.tmprl.cloud/prometheus
